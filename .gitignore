# X のログインセッション。絶対にコミットしない
secrets/

# 環境変数（ANTHROPIC_API_KEY など）
.env
.env.local

node_modules/
.next/
out/
*.tsbuildinfo

# クロール中間物
# crawl-state.json は除外しない。GitHub Actions の定期実行で
# 「前回いつ巡回したか」を引き継ぐ必要があるため（毎回全件巡回すると
# アクセスが過剰になり凍結を招く）。機密は含まない。
data/.cache/

# 画像判別の作業ファイル。ローカルの絶対パスを含むうえ、
# prepare-image-review でいつでも作り直せる
data/image-review-queue.json

# UI 検証のスクリーンショット
screenshots/

.DS_Store
Thumbs.db
.image-review/
data/ocr-cache.json
.tessdata/

.vercel
.env*
